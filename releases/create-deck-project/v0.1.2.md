# @deckio/create-deck-project v0.1.2

**Released:** 2026-03-13

## What changed

### Fixed
- Include `templates/` directory in published npm package. The react-bits templates (aurora, blur-text, decrypted-text, shiny-text, spotlight-card) were missing from the tarball, causing `ENOENT` errors when scaffolding projects with the shadcn design system.

## Upgrade guide

No action required. This fixes the scaffolding tool so it works correctly.
