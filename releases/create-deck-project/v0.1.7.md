# @deckio/create-deck-project v0.1.7

**Released:** 2026-03-13

## What changed

### Fixed
- Fix premature "Project scaffolded!" success message that appeared before `npm install` and engine initialization completed. A spinner now covers all three phases (scaffold → install → init) and success is only shown when each phase finishes.
- Polish TUI prompt copy: shorter, on-point messages (`"Deck title"`, `"Design system"`, `"Appearance"`, `"Deck icon"`, `"Aurora palette"`, `"Accent color"`) and cleaner hint text.
- Update intro and outro text (`✦ DECKIO — new deck` / `✦ Ready! cd <name> && npm run dev`).

## Upgrade guide

No action required.

## Contributors

- @lopezleandro03
